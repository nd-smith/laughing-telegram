# Issue Index

- ~~[0001 — Project scaffolding & dependencies](complete/0001_project_scaffolding_and_dependencies.md)~~ {2026-05-01 → 2026-05-01} [created `pipeline/`, `pipeline/sources/`, `shared/`, `tests/` with empty `__init__.py`; pinned `requirements.txt` with seven deps; ADR 0005]
- ~~[0002 — Envelope construction](complete/0002_envelope_construction.md)~~ {2026-05-01 → 2026-05-01} [added `pipeline/envelope.py` with `build_envelope(...)` (keyword-only args) and `SCHEMA_VERSION = "1.0"`; 11 unit tests in `tests/test_envelope.py`; ADR 0006]
- ~~[0003 — Event validation](complete/0003_event_validation.md)~~ {2026-05-01 → 2026-05-01} [added `pipeline/validation.py` with `validate(raw_event, required_fields)` returning `ValidationResult(ok, reason)` NamedTuple; 7 unit tests in `tests/test_validation.py`; ADR 0007]
- ~~[0004 — Source module contract & loader](complete/0004_source_module_contract_and_loader.md)~~ {2026-05-01 → 2026-05-02} [added `pipeline/sources/__init__.py` with contract docstring and `load_source(name)` (raises `TypeError` naming the offending attribute on contract violations); 6 unit tests in `tests/test_sources.py` using in-memory `sys.modules` fakes; ADR 0008]
- ~~[0005 — Structured JSON logging](complete/0005_structured_json_logging.md)~~ {2026-05-01 → 2026-05-02} [added `shared/logging.py` with `setup_logging(source, level)`, `JsonFormatter`, and `log_context(correlation_id, envelope_id)` contextmanager backed by `contextvars`; 10 unit tests in `tests/test_logging.py`; ADRs 0009, 0010]
- ~~[0006 — Prometheus metrics](complete/0006_prometheus_metrics.md)~~ {2026-05-01 → 2026-05-02} [added `pipeline/metrics.py` declaring all eight instruments per PRD §Observability with default `prometheus_client` buckets, plus `start_metrics_server(port)`; 6 unit tests in `tests/test_metrics.py`]
- ~~[0007 — Event Hub consumer](complete/0007_event_hub_consumer.md)~~ {2026-05-01 → 2026-05-02} [added `pipeline/consumer.py` with `EventHubConsumer` wrapping sync `EventHubConsumerClient` + `BlobCheckpointStore` (DefaultAzureCredential), `run(callback, shutdown: Event)` with after-success checkpointing and watcher-thread shutdown; added `azure-eventhub-checkpointstoreblob==1.2.0` to `requirements.txt`; 7 unit tests in `tests/test_consumer.py`; ADRs 0011, 0012]
- ~~[0008 — OneLake Parquet writer](complete/0008_onelake_parquet_writer.md)~~ {2026-05-01 → 2026-05-02} [added `pipeline/writer.py` with `OneLakeWriter` (buffered Parquet, daemon flusher thread, `start()`/`stop()`/`add()`/`flush()`); pyarrow schema is flat envelope columns + JSON `metadata`/`payload`; ADLS Gen2 path `{lakehouse_path}/{source}/YYYY/MM/DD/{ts}_{batch_id}.parquet`; metrics wired to `batch_write_duration_seconds`, `batch_size`, `buffer_size`; 14 unit tests in `tests/test_writer.py`; ADRs 0013, 0014]
- ~~[0009 — Kafka publisher](complete/0009_kafka_publisher.md)~~ {2026-05-01 → 2026-05-02} [added `pipeline/publisher.py` with `KafkaPublisher` wrapping `confluent_kafka.Producer`; `publish(topic, envelope)` does `produce()` then `flush(timeout)` with per-message `on_delivery` callback recording `kafka_publish_duration_seconds`; `PublishError` surfaces broker failure and flush timeout to caller; strict `json.dumps` (no `default=`); `flush(timeout_s)` for shutdown; 15 unit tests in `tests/test_publisher.py`; ADRs 0015, 0016]
- [0010 — Processing pipeline & retry/DLQ orchestration](0010_processing_pipeline_and_retry_dlq.md) {2026-05-01} — per-event glue, 3-retry exponential backoff, DLQ topic publish
- [0011 — Worker entry point & main loop](0011_worker_entry_point_and_main_loop.md) {2026-05-01} — `pipeline/__main__.py` CLI, wiring, SIGTERM/SIGINT graceful shutdown
